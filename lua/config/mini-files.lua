local mf = require("mini.files")

mf.setup({
  windows = {
    max_number = 1,
    preview = true,
    width_focus = 40,
    width_nofocus = 25,
    width_preview = 60,
  },
  options = {
    use_as_default_explorer = false,
  },
})